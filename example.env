TZ=Europe/Rome

# default (poi li gestiamo da GUI per account multipli)
LE_DEFAULT_EMAIL=

# 1=staging (test), 0=production
LE_DEFAULT_STAGING=1

# Token segreto per chiamare /api/cron/renew
CRON_TOKEN=
